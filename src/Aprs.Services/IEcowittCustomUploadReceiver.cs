namespace Aprs.Services;

public interface IEcowittCustomUploadReceiver
{
    // Future Phase 16/local API work should implement a small HTTP endpoint that accepts
    // Ecowitt custom upload payloads and forwards them into the weather driver framework.
}

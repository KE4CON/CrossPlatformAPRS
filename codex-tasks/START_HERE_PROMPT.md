# First Codex Prompt

You are working in this repository. Read `AGENTS.md`, `README.md`, `docs/FEATURE_SCOPE.md`, and `codex-tasks/MASTER_TASK_LIST.md` before making changes.

Start with Phase 0, Task 0.1 and Task 0.2 only.

Goal:
Create a buildable .NET 8 cross-platform C# solution for a modern amateur-radio APRS desktop client using Avalonia.

Important:
- Do not implement APRS protocol logic yet.
- Do not copy UI-View assets or text.
- Keep code separated according to AGENTS.md.
- Add or update tests where practical.
- After changes, run `dotnet restore`, `dotnet build`, and `dotnet test`.

When finished, summarize:
- files changed
- build/test results
- remaining TODOs
